#include <stdio.h>

/**
 * main - Entry point
 * Return: 0
 */

int main ()
{
	int a = 2;
	int b = 3;
	int result = a * b;

	if ("%d %d", a * b)
	{
		printf("You're correct %d is the result\n", result);
	}

	else{
		printf("Not correct");
	}
	return (result);
}
