#include <stdio.h>

/**
 * main - Entry point
 * Return: 0
 */

int main (result)
	int a = 2;
	int b = 3;
	int a * b = result;

	if ("%d %d", &a * &b)
{

	a * b = mul;
		printf("Correct answer");
                return (result);
} else{
   printf("not correct")
}
